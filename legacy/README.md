# Legacy UI removed

The Next.js frontend was removed in v4.4. All daily-use and admin features live in **`ui/`** (Vite + React):

- Settings → AI credentials, default models, cloud sync
- `/transformations` → run + create/delete transformations
- `/podcasts` → generate + list episodes

Use `./scripts/peeknook-dev.sh` for local development.
